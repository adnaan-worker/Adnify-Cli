# Adnify-Cli 任务清单

## 角色分工

- 开发者 A：Agent / 工具执行 / 模型能力
- 开发者 B：持久化 / 启动恢复 / 文档 / 交互体验

---

## 里程碑

| 代号 | 目标 | 当前判断 |
|------|------|----------|
| M1 | 会话可持久化到本地，退出再开能继续协作 | 基本完成 |
| M2 | 模型可调用工具，结果能回到对话 | 开发中 |
| M3 | 多轮工具 + 持久化 + UI 协作链完整可用 | 待开始 |

---

## A 线：Agent 与工具

- [ ] 接入真实工具执行能力，并与 `prompts/` 中的工具定义对齐
- [ ] 扩展 `ModelGatewayPort` / `AiSdkGateway`，支持工具定义、工具调用与结果回注
- [ ] 打通 Agent 模式下的多轮工具编排链路
- [ ] 保持 `chat / agent / plan` 模式并存且切换清晰
- [ ] 补工具相关测试
- [ ] 设计工具权限与审批占位机制

---

## B 线：持久化与体验

### 已完成

- [x] 会话文件化持久化
- [x] 启动时按工作区恢复最近会话
- [x] `createRuntime` 与启动流程接好
- [x] `:session` 命令
- [x] `:sessions` / `:resume` 会话管理能力
- [x] 自定义数据目录与跨平台存储路径解析
- [x] `:storage` / `:storage set` / `:storage reset`
- [x] `:config` 命令式配置链路
- [x] README 已补存储、配置、会话行为说明
- [x] 输入体验第一轮优化

### 待继续

- [ ] 清理国际化文本编码问题，尤其是 `AppI18n.ts`
- [ ] 再优化 `sessions` 展示与命令进入会话流的呈现
- [ ] 做一轮终端渲染稳定性回归检查
- [ ] 补 README 的产品化展示细节与示例截图
- [ ] 视情况补一份存储/配置专题文档

---

## 推荐合并顺序

1. B 线先把 M1 收口，保证文档、存储、恢复、基础交互稳定
2. A 线推进工具执行链和 Agent 能力，进入 M2
3. A/B 一起做 UI 与多轮协作整合，验证 M3

容易冲突的文件：

- `src/createRuntime.ts`
- `src/presentation/ink/hooks/useCliController.ts`
- `src/application/dto/AdnifyCliRuntime.ts`

如果两人同时修改这几处，建议先对齐责任边界再合并。

---

## 当前建议

### 对开发者 A

- 优先做工具执行链，不要再停留在静态 Prompt 定义
- 先完成最小可用的工具调用闭环，再做权限与审批

### 对开发者 B

- 先清国际化编码问题
- 再继续打磨 sessions 区和命令面板体验
- 最后补一轮 README / 文档 / 示例展示

---

## 进度勾选

- [x] M1
- [ ] M2
- [ ] M3
