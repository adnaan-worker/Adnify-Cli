# Adnify-Cli 任务清单

**开发者 A**：Agent / 工具 / 模型  
**开发者 B**：持久化 / 启动恢复 / 文档与体验  

---

## 里程碑

| 代号 | 功能目标 |
|------|----------|
| M1 | 会话可保存到本地，退出再开能接着聊 |
| M2 | 模型能调用只读工具（如读文件、列目录），结果能回到对话 |
| M3 | 多轮工具 + 持久化同时可用 |

---

## A：Agent & 工具

- [ ] 工具执行能力（端口 + 实现），与 `prompts/tools` 里的工具 id 对齐  
- [ ] **模型网关**：扩展 `ModelGatewayPort` / `AiSdkGateway`（tools 定义、tool 调用与结果回注；纯聊天路径保持可用）  
- [ ] 多轮 tool 编排用例（Agent 模式走这条链路）  
- [ ] 与现有聊天模式并存，按模式切换
- [ ] 工具相关单测（路径安全、基本执行）  
- [ ] （可选）工具审批占位，默认全自动通过  

---

## B：持久化 & 体验

- [ ] 会话落盘（文件或 SQLite，团队定一种）  
- [ ] 启动时恢复最近会话或按工作区恢复（最小可用策略即可）  
- [ ] 与 `createRuntime` / 启动流程接好  
- [ ] （可选）`:session` 命令，显示当前会话信息  
- [ ] README：存储位置、环境变量、行为说明  
- [ ] 持久化单测  

---

## 合并顺序（建议）

1. B：持久化 + 恢复（M1）  
2. A：工具 + Agent 链路（M2）  
3. 一起：UI 里按模式走不同用例 + 验证 M3  

易冲突文件：`createRuntime.ts`、`useCliController.ts`、`AdnifyCliRuntime.ts` — 两人改前先对齐或由主导合并。

---

## 进度

- [ ] M1  
- [ ] M2  
- [ ] M3  
